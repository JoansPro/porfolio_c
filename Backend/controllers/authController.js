const Admin = require('../models/Admin');\n\n// Replace the store usage with Admin model usage\n//... (rest of the authController logic)\n\nconst JWT_SECRET = process.env.JWT_SECRET; // Ensure JWT_SECRET does not fallback to 'your_jwt_secret_here'\n\n// Export the necessary methods from the controller\nmodule.exports = { ... };